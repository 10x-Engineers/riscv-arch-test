///////////////////////////////////////////
// SvH/SvH_hfence_gvma_mode_HSmode-00.S
//
// This file is autogenerated by testgen.
// Do not modify directly.
//
// Copyright (C) 2025 Harvey Mudd College & Oklahoma State University, UET Lahore, Habib University
// SPDX-License-Identifier: Apache-2.0
///////////////////////////////////////////

##### START_TEST_CONFIG #####
# REQUIRED_EXTENSIONS: ['I', 'H']
# # # no param constraints
# MARCH: rv${XLEN}ih_zicsr_zifencei
##### END_TEST_CONFIG #####

// Define the size of the signature region
#define SIGUPD_COUNT 12

// Extra defines (if needed) to enable trap handlers, fpu, vector, etc.
#define RVTEST_HYPERVISOR
#define TRAP_SIGUPD_COUNT 4096

// Include framework macros
#include "riscv_arch_test.h"

// Set up test environment and declare main entry point
RVTEST_BEGIN



/////////////////////////////////
// hfence_gvma_mode_HSmode
//   SvH coverpoint stimulus
/////////////////////////////////



/////////////////////////////////
// hfence_gvma_mode_HSmode
//   SvH directed coverpoint stimulus
/////////////////////////////////
#ifdef SV32_SUPPORTED
  .set gpa_data, 0x00C002000
  .set gpa_code, 0x80000000
  SUPERPAGE_G_PTE_SETUP(sv32x4, rvtest_code_begin, (PTE_D | PTE_A | PTE_U | PTE_X | PTE_R | PTE_V), gpa_code, LEVEL1)
  G_PTE_SETUP(sv32x4, rvtest_hlvl0_pg_tbl, (PTE_V), gpa_data, LEVEL1)
  G_PTE_SETUP(sv32x4, test_region, (PTE_D | PTE_A | PTE_U | PTE_W | PTE_R | PTE_V), gpa_data, LEVEL0)
  csrw vsatp, x0
  csrw hgatp, x0
  hfence.gvma
  LI(t0, 0x257a0911)
  la t1, test_region
  sw t0, 0(t1)

// --------------------------------------------------------------------------------------------------------------------------------
// Test Cases Start from here
// --------------------------------------------------------------------------------------------------------------------------------

//--------------------------------------------------------------------------------------------------------------------------------
// Test case 1: hgatp Bare to sv32x4 plus HFENCE.GVMA x0,a6; HS HLV | Expected: Successful
//--------------------------------------------------------------------------------------------------------------------------------
  HGATP_SETUP(sv32x4)
  csrr t0, hgatp
  LI(t1, 1)
  slli t1, t1, 22
  or t0, t0, t1
  csrw hgatp, t0
  RVTEST_GOTO_LOWER_MODE HSmode
  LI(a6, 1)
  hfence.gvma x0, a6
  LI(t1, gpa_data)
  hlv.w a3, (t1)
  nop
  RVTEST_GOTO_MMODE
  test1_bare_to_paged:
  RVTEST_SIGUPD(x2, x5, x4, a3, test1_bare_to_paged, test1_bare_to_paged_str)

//--------------------------------------------------------------------------------------------------------------------------------
// Test case 2: hgatp sv32x4 to Bare plus HFENCE.GVMA; HS HLV of SPA | Expected: Successful
//--------------------------------------------------------------------------------------------------------------------------------
  LI(t0, 0x257a0912)
  la t1, test_region
  sw t0, 0(t1)
  csrw hgatp, x0
  RVTEST_GOTO_LOWER_MODE HSmode
  hfence.gvma
  la t1, test_region
  hlv.w a3, (t1)
  nop
  RVTEST_GOTO_MMODE
  test2_paged_to_bare:
  RVTEST_SIGUPD(x2, x5, x4, a3, test2_paged_to_bare, test2_paged_to_bare_str)

  .pushsection .data
  .p2align 12
test_region:
  .word 0
  .word 0
  .word 0
  .word 0
  .p2align 12
rvtest_hlvl0_pg_tbl:
  .zero 4096
  .p2align 12
rvtest_vlvl0_pg_tbl:
  .zero 4096
test1_bare_to_paged_str: .string "\"Mismatch on Bare to Paged HFENCE.GVMA HLV\""
test2_paged_to_bare_str: .string "\"Mismatch on Paged to Bare HFENCE.GVMA HLV\""
  .popsection

#endif  // SV32_SUPPORTED
#ifdef SV39_SUPPORTED
  .set gpa_data, 0x00C002000
  .set gpa_code, 0x0000000080000000
  SUPERPAGE_G_PTE_SETUP(sv39x4, rvtest_code_begin, (PTE_D | PTE_A | PTE_U | PTE_X | PTE_R | PTE_V), gpa_code, LEVEL2)
  G_PTE_SETUP(sv39x4, rvtest_hlvl1_pg_tbl, (PTE_V), gpa_data, LEVEL2)
  G_PTE_SETUP(sv39x4, rvtest_hlvl0_pg_tbl, (PTE_V), gpa_data, LEVEL1)
  G_PTE_SETUP(sv39x4, test_region, (PTE_D | PTE_A | PTE_U | PTE_W | PTE_R | PTE_V), gpa_data, LEVEL0)
  csrw vsatp, x0
  csrw hgatp, x0
  hfence.gvma
  LI(t0, 0x257a0911)
  la t1, test_region
  sw t0, 0(t1)

// --------------------------------------------------------------------------------------------------------------------------------
// Test Cases Start from here
// --------------------------------------------------------------------------------------------------------------------------------

//--------------------------------------------------------------------------------------------------------------------------------
// Test case 1: hgatp Bare to sv39x4 plus HFENCE.GVMA x0,a6; HS HLV | Expected: Successful
//--------------------------------------------------------------------------------------------------------------------------------
  HGATP_SETUP(sv39x4)
  csrr t0, hgatp
  LI(t1, 1)
  slli t1, t1, 44
  or t0, t0, t1
  csrw hgatp, t0
  RVTEST_GOTO_LOWER_MODE HSmode
  LI(a6, 1)
  hfence.gvma x0, a6
  LI(t1, gpa_data)
  hlv.w a3, (t1)
  nop
  RVTEST_GOTO_MMODE
  test1_bare_to_paged:
  RVTEST_SIGUPD(x2, x5, x4, a3, test1_bare_to_paged, test1_bare_to_paged_str)

//--------------------------------------------------------------------------------------------------------------------------------
// Test case 2: hgatp sv39x4 to Bare plus HFENCE.GVMA; HS HLV of SPA | Expected: Successful
//--------------------------------------------------------------------------------------------------------------------------------
  LI(t0, 0x257a0912)
  la t1, test_region
  sw t0, 0(t1)
  csrw hgatp, x0
  RVTEST_GOTO_LOWER_MODE HSmode
  hfence.gvma
  la t1, test_region
  hlv.w a3, (t1)
  nop
  RVTEST_GOTO_MMODE
  test2_paged_to_bare:
  RVTEST_SIGUPD(x2, x5, x4, a3, test2_paged_to_bare, test2_paged_to_bare_str)

  .pushsection .data
  .p2align 12
test_region:
  .word 0
  .word 0
  .word 0
  .word 0
  .p2align 12
rvtest_hlvl1_pg_tbl:
  .zero 4096
  .p2align 12
rvtest_hlvl0_pg_tbl:
  .zero 4096
  .p2align 12
rvtest_vlvl1_pg_tbl:
  .zero 4096
  .p2align 12
rvtest_vlvl0_pg_tbl:
  .zero 4096
test1_bare_to_paged_str: .string "\"Mismatch on Bare to Paged HFENCE.GVMA HLV\""
test2_paged_to_bare_str: .string "\"Mismatch on Paged to Bare HFENCE.GVMA HLV\""
  .popsection

#endif  // SV39_SUPPORTED

// Instantiate trap handlers and other epilog code, call RVMODEL_HALT at end of test
RVTEST_CODE_END

// Create scratch data sections and label for test data
RVTEST_DATA_BEGIN

// Test data


// Testcase strings


// End of test data section and model specific data
RVTEST_DATA_END

// Signature region for self-checking
RVTEST_SIG_SETUP

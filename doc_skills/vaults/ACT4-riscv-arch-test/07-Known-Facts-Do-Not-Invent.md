# Known facts — do not invent / do not reverse

Agents: if a claim contradicts this list, **re-read files** or ask — do not “remember” the opposite.

## Pipeline truths
- [x] TB Tracefile = `*.rvvi`, **not** Sail `*.trace`
- [x] Converter emits `MODE_VIRT` + SvH PTE/access keys + **`TRAP`** (fetch-fault → `*_PTE_I`); interrupts / PHYS_ADR still absent
- [x] `coverpoints/coverage/*` is autogenerated glue — never hand-edit bins there
- [x] `EXTENSIONS=<Suite>` selects tests **and** covergroup define; not a single bin
- [x] Default `EXCLUDE_EXTENSIONS` includes `Sm`

## Host / tooling
- [x] Sail used here is nadime15 hypervisor branch, sim **v0.13**
- [x] Questa = Mentor/Siemens **2021.2_1** at `~/questasim/questasim/` — `env_questa.sh` via `setup_act4.sh` / `~/.bashrc`
- [x] Python tooling = `~/.local/bin/uv` → Make `uv run act` — **no** `.localenv` activate
- [x] License path **only** `~/questasim/questasim/license.dat` — never `questa-starter-official` / Intel FPGA Starter
- [x] Intel FPGA Starter paths are out of scope for this workstream
- [x] ACT4 checks **`riscv64-unknown-elf-gcc`**, not host `gcc`. **This checkout** now requires GCC **≥15** (`/opt/riscv-gcc-15` **16.1.0**). Host 13.2 fails `check_compiler_version`. Shell: `gcc15` or `export PATH=/opt/riscv-gcc-15/bin:$PATH` before Make.

## Test / env bugs already fixed (keep)
- [x] Every `tests/priv/SvH/*.S` header: `Coverpoints mapped (SvH_cg):` + exact coverpoint ids only (no sheet/xlsx/column text in the `.S`), plus `// What this proves (one line):` + indented summary (restored 2026-08-11 — required SvH header style).
- [x] `coverpoints/priv/SvH_coverage.svh` comments are **front-facing clean** (2026-08-07): no sheet refs (M1/M2/…), no OBS-BLOCK/MISS/MOD/SoT/Implementation_Plan jargon. Empty-bin notes must cite concrete root cause (Sail path/symbol, `sail_to_rvvi.py`, or stimulus) — no vague “Sail may…”. Backend planning tags stay in vault/plan/xlsx only — not in this `.svh`.
- [x] **Sail tree in this workspace:** `sail-riscv-hypervisor/` (nadime15 `hypervisor`, remote `https://github.com/nadime15/sail-riscv`). Key SvH citations (2026-08-07 comment rewrite):
  - VSBE hardwire: `model/core/sys_regs.sail::legalize_hstatus` — VSBE assign commented out (“We don't currently support changing VSBE”)
  - PBMT/PBMTE: `model/sys/vmem_pte.sail::pte_is_invalid` + `currentlyEnabled(Ext_Svpbmt)`; apply/force PMA in `model/sys/vmem.sail::pt_walk`
  - Reserved PTE: `model/core/isa_version.sail::pte_reserved_bits_must_be_zero` (Priv ≥ 1.12) + `pte_is_invalid` (`pte_ext[reserved]`)
  - A/D Svade: `vmem.sail::update_and_write_pte` → `PTW_PTE_Needs_Update`; exception class in `vmem.sail::build_exception_context` (Explicit VS-Stage = page fault; G-stage / Implicit → `vmem_ptw.sail::convertToGuestException`)
  - TRAP in `.rvvi`: `sail_to_rvvi.py` emits `TRAP 1` from Sail `trapping from …`; fetch-*-fault walks after redirect → `*_PTE_I` (2026-08-07)
- [x] SvH tests follow Sv framework writing style (case list + banners + per-case SIGUPD + mismatch strings + `What this proves`). Keep self-explanatory comments. Do **not** SIGUPD inside VS while vsatp/hgatp paging is ON — return to M first (same pattern as `sv32_spage_Smode.S`).
- [x] **SvH comment completion (2026-08-07):** all 39 `tests/priv/SvH/*.S` have public-reader block/line comments (privilege hop, PTE bits, allow/fault, SIGUPD meaning). No OBS-BLOCK/MISS/`sheet:` jargon in `.S` comments.
- [x] **SvH line comments (2026-08-10):** every active `tests/priv/SvH/*.S` code/data line has a trailing `//` (gold: `svh_two_stage_rw_VSmode.S` / `svh_g_adbit_VSmode.S`). `#define` `\` bodies use `/* */` before `\`` — `//` would eat the backslash.
- [x] **SvH review restructure (2026-08-11):** all **76** `.S` share Sv section order (VA/GPA → PTE → enable/fences → Test Cases Start → DATA banners); no logic/coverage change. Team doc: `riscv-arch-test/SvH_REVIEW_WALKTHROUGH.md`. Smoke 8/8 PASS (`work-svh-smoke`). Denser by design: `vsstatus_mxr_sum_*`, `vs_perm_*`, `*_pte_attr_RV64_*`.
- [x] **SvH anti-AI cleanup (2026-08-11):** removed copy-paste `// Mode hops: T-SBI enter; …` from 72 files (kept file-specific MPRV/VU T-SBI notes only); no `svh_helpers.inc` / `SVH_*` macros. ELF compile OK; Sail schema fail on some CSR sims is host `sail.json` (missing `H`), not `.S` source.
- [x] **SvH per-line comments restored (2026-08-11):** helper expand had stripped trailing `//` on enable/PTE/fence/preload lines. Restored self-explanatory comments on all 76 `.S` (gold style). Inside `#define … \` bodies use `/* */` only — never `//` (eats `\`).
- [x] **SvH in-body `Test case N:` banners (2026-08-12):** every header `//  N.` must have a matching `// Test case N: … | Expected: …` banner in the body (not only case 1). Comments-only; no insn change. Gold: `svh_hfence_gvma_ops_{,sv39_}HSmode.S` cases 1–3. Grouped walks may use `Test cases 3a–3h` / `8d/9`.
- [x] **SvH `_park/` gone (2026-08-11):** `tests/priv/SvH/_park/` deleted — held only a stub pointer for `svh_twostage_invalid_VSmode.S` after unpark; active ELF lives in `tests/priv/SvH/`. Nothing builds from `_park`.
- [x] **Do not edit** `tests/env/rvtest_trap_handler.h` (user 2026-08-12). Local handler patches reverted to git HEAD. **Workaround:** all 76 SvH `.S` use `RVTEST_GOTO_MMODE` (a0=0) instead of `RVTEST_TSBI_GOTO_MMODE` when returning from VA≠PA guest — stock TSBI a0=1 mret at GVA → illegal insn in M.
- [x] **RVVI coverpoints on pte/gpte (2026-08-17):** `RISCV_trace_data` holds `rvvi_mem_access_t mem_i`/`mem_d` only. SvH samples `ins.current.mem_{i,d}.pte` / `.gpte`.
- [x] **SvH line comments (2026-08-21):** All seven SvH generator modules (`SvH.py`, `SvHCommon.py`, `SvH_{twostage,csr,perm,fault,misc}.py`) have EOL `#` comments on code lines for non-Python readers. Verified `make testgen EXTENSIONS=SvH` → **39** `.S`.
- [x] **SvH clear helper names (2026-08-21):** `SvHCommon` public API uses verbs (`build_twins`, `goto_vs`, `enable_two_stage`, `twin_data`, …). Removed dead aliases (`emit_nop_pad`, `mark_scenario`, `emit_pt_data_section`, `GuestMode`, …). Family private helpers prefer `_asm_*`. Entry still `_SCENARIOS` in `SvH.py`. Verified `make testgen EXTENSIONS=SvH` → **39** `.S`.
- [x] **SvH structure like ExceptionsSm (2026-08-21):** `SvH.py` lists `_SCENARIOS` explicitly (no `ALL_GENERATORS` / `_collect`). Twin wrap via `SvHCommon.emit_twin` for twostage/csr/perm/fault; misc keeps `Body` + `_emit_twin`. Verified `make testgen EXTENSIONS=SvH EXCLUDE_EXTENSIONS=Sm JOBS=1` → **39** `.S`.
- [x] **SvH Python emitter comments (2026-08-20):** `SvH.py`, `SvHCommon.py`, `SvH_twostage.py`, `SvH_csr.py` already had dense EOL comments. **`SvH_fault.py` rewritten** with line-level comments on imports, AccessCase fields, every case setup line, and twin reset. **`SvH_misc.py` / `SvH_perm.py`**: imports, helpers (`_emit_twin`, `_vs_only_maps`, `_set_mxr_sum`, guest hops), `_body_g_adbit`, `_body_vsbe`, and `ALL_GENERATORS` annotated by hand (no auto-comment script). Some long packed case-matrix lines in misc/perm bodies remain dense — expand further if a reviewer asks. `make testgen EXTENSIONS=SvH` still **39** `.S`.
- [x] **`make clean` / `clean-tests` drop generated SvH (2026-08-17):** remove `tests/priv/SvH/*.S` and `$(WORKDIR)/stamps/testgen.stamp` so the next `make testgen EXTENSIONS=SvH` regenerates (avoids empty dir + stale stamp). SoT = Python emitters (`SvH.py` + `SvHCommon` + `SvH_{twostage,csr,perm,fault,misc}.py`). `make testgen EXTENSIONS=SvH` → **39** `SvH_<logical>-00.S` (`#ifdef SV32/SV39_SUPPORTED`). **No** materialize / runtime seed I/O. Offline archive: `generators/testgen/src/testgen/priv/extensions/_park/svh_seed/`. Twin `SIGUPD_COUNT` = max(active branch). Permission matrices must mirror seed case counts (not full XWR explosion). Licensed Questa gate: RV32 **99.66%** (574/575) / RV64 **99.71%** (888/889); only `vsbe_hstatus.set`. Design: `docs/superpowers/specs/2026-08-17-svh-testgen-design.md`.
- [x] **SvH testgen 2026-08-19:** comment edits dropped `def _vsatp_body` (`NameError`) and left `generate_hgatp_fault_VSmode` off `SvH_fault.ALL_GENERATORS` (38 files). Restored both. `make testgen EXTENSIONS=SvH EXCLUDE_EXTENSIONS=Sm JOBS=1` → 39 `.S`.
- [x] **DV monthly AI stack brief (2026-08-24):** `docs/DV-Monthly-AI-Agent-Stack-and-Testgen-Demo-Plan.md` — inventory, sub-agents, **§11** path catalog. **§4.0–§4.2** = xlsx/csv → Plan → approval → Python → Walkthrough.
- [x] **SvH-Testgen-Plan.md (2026-08-25):** synced to commented `SvH*.py` — §1b module map, expanded `SvHCommon` helpers, §5.0 master index with live `SIGUPD_COUNT` / case banners from regenerated `.S`.
- [x] **act4-testgen skill (2026-08-24):** Project skill `.cursor/skills/act4-testgen/` (lifecycle + reuse-map). Use for SvH/other suite generation; do not hand-edit generated `.S`. Run/coverage remains `act4-riscv-arch-test`; xlsx/norms remains `act4-hypervisor`. Ask user only when a decision cannot be inferred.
- [x] **SvH Walkthrough format (2026-08-25):** `docs/SvH_Testgen_Codebase_Walkthrough.md` — T01–T39 end with **Why those instructions (mapping rules)** after assembly spine; global missing deps in §7 only.
- [x] **AI_CONTEXT_SUMMARY (2026-08-25):** `riscv-arch-test/AI_CONTEXT_SUMMARY.md` refreshed — **39** Python chunks (not 76 legacy files), three-phase testgen docs, `make testgen` in commands; trimmed duplicate historical sections (detail stays in this vault).
- [x] **SvH docs refresh (2026-08-24):** pipeline + `_SCENARIOS`/`build_twins` (not `_collect`/`emit_twin`). Seeds `_park/svh_seed/` offline only.
- [x] **RVVI vs ACT4:** Upstream puts leaf PTEs on **`rvvi_mem_access_t` / TEXT `MEM` (`PTE`/`GPTE`)**. ACT4 now stores H PTEs that way on the TRACE sideband; coverpoint field names remain the old shim names. Map: skill `RVVI-for-ACT4.md`.
- [x] SvH T-SBI: **return from guest VA≠PA** → `RVTEST_GOTO_MMODE` (not TSBI a0=1). **Enter:** PA/identity → `RVTEST_TSBI_GOTO_VSMODE`/`_SMODE`; guest VA≠PA (`V_SAVE_AREA_SETUP`) → `RVTEST_GOTO_LOWER_MODE`.
- [x] SvH `SIGUPD_COUNT` must equal the exact number of `RVTEST_SIGUPD` / `RVTEST_SIGUPD_CSR_READ` dumps (count walk-loop iterations). Do not leave padded headroom like 8/30/80.
- [x] Do **not** call `RVTEST_SIGUPD` while MPRV redirects the signature access through a page table that does not map `begin_signature` (`svh_mprv_vsatp_*`: vsatp maps only `va_s`/`va_u`). Signature generation stores (cause 15) but self-check loads (cause 13) → trap-cause mismatch. Clear MPRV before the dump, re-set it for the next case. `svh_mprv_hgatp_*` is fine — its G-stage map reaches the signature.
- [x] `build/*.sig.log` is the **signature-generation** run only; it prints SUCCESS even for tests whose self-check fails. Verify real pass/fail via `work/<cfg>/coverage/priv/<Suite>/<test>.log` (`TEST FAILED`) or a full `make coverage`.
- [x] RV32 `svh_csr_{vsatp,hgatp}_fields_HSmode.S`: do **not** double-write MODE-only as a fake “PPN=0” case — one write covers both; PPN all-1s gets its own readback before the walk. RV64 twins had no that dup; hgatp RV64 now also SIGUPD all-1s before walk. CSR PPN/MODE walks dump each iteration (Sv-style).
- [x] `cp_hgatp_mprv_effects`: sheet needs **5** MPP/MPV axes (`_m/_hs/_vs/_u/_vu`) + separate `_hlv`. VS/VU crosses keep `g_pte_xwr100`; M/HS/U do **not** (no G walk when MPV=0 / MPP=M). Stimulus: `svh_mprv_hgatp_Mmode.S` (+ RV64 `svh_mprv_hgatp_sv39_Mmode.S`).
- [x] `sail_to_rvvi.py` data-walk cap: stop after enough **leaf** PTEs (not fixed count 2) — Sv39 single-stage needs 3 `mem[R]` walks; old `max_walk=2` dropped the leaf and left RV64 MPRV/HLV crosses ZERO.
- [x] RV64 twins for listed P0/P1 gaps: `svh_mprv_vsatp_sv39_Mmode.S`, `svh_mprv_hgatp_sv39_Mmode.S`, `svh_tvm_hgatp_RV64_HSmode.S` (MPV via `mstatus`, not `mstatush`).
- [x] `actual_tramp_sz` in HEAD is `((UDB_MXLEN + 3*NUM_SPECD_INTCAUSES + 9 + 5) * 4)`. Do not retune it in `rvtest_trap_handler.h`.
- [x] HS `adj_epc_rtn` must `hlvx.hu` when SPV=1 (guest sepc)
- [x] Never `sudo make` / `sudo make clean` in this tree — `work/` and generated `tests/rv32*` become root-owned and break later builds. Fix: `sudo chown -R "$USER:$USER" .` (or delete `*.root-owned` leftovers after reclaim)
- [x] `sail-rv64-max/sail.json`: keep ACT4 settings (`vlen_exp` 10, Zaamo/Zba/… on, PMP 64, `max_time_to_wait` 200); enable H with **surgical** Sail 0.13 schema adds only — never wholesale-replace with Sail default
- [x] Hact4 Sail-tuned tests in `tests/priv/Hact4/` all **PASS** + emit `.rvvi` (no `_park`)
- [x] Hact4 bring-up covergroups exist: `coverpoints/priv/Hact4_coverage.svh` (+ `_init.svh`); glue wires `HACT4_COVERAGE` / `hact4_sample`

## Proven artifacts
`work/sail-rv32-max/coverage/priv/Hact4/`: PASS `.rvvi` + `Hact4.ucdb` + reports. `Hact4_cg` **80%** — all bins hit except `cp_hsv` (hsv_w/h/b = 0; Sail `.trace` may have HSV but `sail_to_rvvi.py` often omits them).

**SvH P0/P1 directed FC Cleared (2026-08-02); P2–P4 + OUR-side pad + RV64 twin + OUR residual close (2026-08-07):**
- RV32: `SvH_cg` **98.49%** — **36/36 TEST PASSED** (unchanged; Sail-only residual)
- RV64: `SvH_cg` **98.73%** (was 96.00% → was 74.94%) — **37/37 TEST PASSED** after vs_perm + mxr_sum Sv39 twins
- **RV64 OUR residual close (2026-08-07):** `svh_vs_perm_sv39_{VS,VU}mode.S`, `svh_vsstatus_mxr_sum_sv39_{VS,VU}mode.S` (+ case 10b lw-only pad on VS mxr twin)
- **Cleared on RV64 this residual close:** `cp_vsstatus_mxr_sum_rw/_x` **100%**; `cp_vsatp_spages_sum_rw/_x` **100%**; `cp_vsatp_perm_checks_rw/_x` **100%**
- **Prior RV64 twins (17):** hfence*; g_perm VS/VU; g_u_bit; g_adbit; sum_Upages; xonly_mxr0×4; vu_rwx; twostage_invalid; mprv_sum; vsatp/hgatp_fault
- **Sail-only remain (do not fake):** VSBE (`legalize_hstatus`); endianess BE. **Adbit 2026-08-10:** `svh_g_adbit_{,sv39_}VSmode.S` is VS two-stage implicit G A/D (G PTE for VS L0: A=D=0 R/W U=1; VS leaf A=D={11,00} XWR=111; lw/sw/jalr). ADUE=0 (Svade): implicit G → guest-page 21/23/20; VS AD=00 → page-fault 13/15/12. Both ELFs **TEST PASSED**. Coverpoint `g_pte_ad_unset` is now `8'b00??0111` (not XWR=111). Converter `stage()` also swaps `[G U=1 R+W, VS U=0 leaf]`. Re-run licensed Questa before a new SvH_cg %.
- Status xlsx (**tracker 2026-08-20**): repo-root `SvH Sail Testing Status latest up to date.xlsx` — **39** rows 1:1 with `tests/priv/SvH/SvH_*-00.S` (Python testgen); cols L–O = Family / module / `generate_*` / twin layout. Coverage **99.29/99.39** unchanged; Partial = VSBE only. Old **76**-row twin-split `svh_*.S` inventory superseded on disk.
- [x] **SvH arch vs Gap Highlighted (2026-08-11 staff audit):** RV64-only Col-A + RV64 Env gated `ifdef UDB_MXLEN_64`. `svh_csr_vsatp_fields_HSmode.S` **kept** (R5/R6 BOTH). No arch-mismatched `.S` deleted. VU axis for `cp_two_stage_mxr` added (sheet R43).
- Fetch-fault recovery requires `jalr ra, …` (handler sets xEPC=ra); V=0 ifetch leaves need **X set** for `invalid_pte` bin `11??1??0`
- **Svade** on — G A=0 must fault; u_mode/g_bit data leaves still **XWR=111**; **adbit implicit G** uses **R/W no X** (`8'b00??0111`)
- SIGUPD after MMODE when hgatp ON; sentinels in `s*` after T-SBI
- Two-stage VS A=0 under Svade → Sail **does** raise explicit VS-stage **page fault** (not guest-page) via `build_exception_context`. Implicit G A=0 on the VS-PT map → **guest-page** (`convertToGuestException`). Do not require both on one insn; `cp_hgatp_adbit_behavior` × `cp_hgatp_adbit_vs_ad_*` split.
- [x] **Never `hfence.gvma` in VS/VU** — Sail (and the ISA) take a virtual-instruction exception (cause 0x16). Flush G TLB only from M or HS. Fixed 2026-08-07 in `svh_g_perm_VSmode.S` / `svh_g_perm_VUmode.S` (case-1 sw/lw now split by an M-mode `hfence.gvma`).
- [x] Store-fault SIGUPD must check **SPA** (or a distinct surviving value), not a zeroed store-source register — otherwise a wrongly successful store still “passes”. Strengthened in g_perm VS/VU, vs_perm VS/VU, vsstatus_mxr_sum VU (2026-08-07).
- [x] Same-hop `lw` then `sw` (or HLV then HSV) often TLB-hits → silent walk → empty `*_PTE_D` bins; use separate hops + `hfence.vvma`/`hfence.gvma` from M/HS.

## Open / future
- [x] OUR-side mxr_sum / spages / reserved / twostage (2026-08-07 pad close) — stimulus maxed on RV32
- [ ] Remaining Partial (Sail-only): VSBE needs Sail
- [x] `VM_permission_invalid_*` write_acc (2026-08-11) — VS sw cases 3–4 on twostage_invalid twins; Covered 100% both arch
- [ ] Sheet R48 `VM_permission_invalid` F asks `{HS,VS,U,VU}` — FC still VS-directed; 2026-08-12 added `_x` fetch crosses (need jalr ELFs). HS/U/VU still no ELF.
- [ ] Sheet R43 LB/LH/LD + “not x-only” halves not encoded. 2026-08-12 `cp_two_stage_mxr` now crosses `mxr_sstatus` × VS x-only × G x-only × `{VS,VU}` (sheet axes that `.rvvi` can sample).
- [x] **SvH_cg licensed coverage (2026-08-12):** RV32 **96.33%** (`work/sail-rv32-max/reports/SvH_uncovered.txt`); RV64 **95.81%** (`work/sail-rv64-max/reports/SvH_uncovered.txt`). Both `make coverage … EXTENSIONS=SvH` exit 0. Residual ZERO: VSBE `set`, HFENCE rs1/rs2 nz, `VM_permission_invalid_*_x`, MODE-field matrix (RV64), new `_x` exec siblings.
- [x] Adbit sheet path (2026-08-10): VS implicit G A/D + VS leaf AD=00; ELFs PASS; **Questa re-measured 2026-08-11**
- [x] P2 ELFs + VU packs + TRAP converter + invalid_pte_x / perm_checks_rw / exception_reporting (2026-08-07)
- [x] P3/P4 solvable ELFs+crosses (2026-08-07) — twostage_invalid unparked PASS; `_park/` removed 2026-08-11
- [x] mprv_sum write + gpa_width write + PBMT no_support pads + reserved walk (2026-08-07 follow-up)
- [~] `svh_vsbe_endian_{,sv39_}VSmode.S` — ELF PASS (RV32+RV64 twins); BE bins Sail-blocked (documented, not faked)
- [x] `.rvvi` TRAP emit for `hgatp_exception_reporting` (2026-08-07)
- [x] Port hfence/fault/g_perm/sum/xonly/vu_rwx/mprv_sum ELFs to RV64 (2026-08-07 twin pass → 96.00%)
- [x] **SvH Testgen Review Audit (2026-08-31):** Refactored all SvH Python generator modules (`SvHCommon`, `SvH_csr`, `SvH_fault`, `SvH_misc`, `SvH_perm`, `SvH_twostage`, `SvH`). Replaced dynamic wrapper functions `goto_*()` with declarative constants `GOTO_VS`, `GOTO_VU`, `GOTO_HS`, `GOTO_MMODE`. Cleaned up `make_svh()` explicit scenario emits, eliminated custom signature wrappers/mismatch strings, and verified 237/237 tests PASS cleanly under GCC 15 with **99.66% coverage**.
- [x] Port `svh_vs_perm_*` + `svh_vsstatus_mxr_sum_*` to Sv39 (2026-08-07 → RV64 **98.73%**; OUR residual closed)
- [ ] Grow Hact4 / ExceptionsH beyond bring-up
- [x] P0/P1 FC clearance + status xlsx
- [x] Sail does **not** allow changing `hstatus.VSBE` (`legalize_hstatus` in `sail-riscv-hypervisor/model/core/sys_regs.sail`)
- [x] Converter: two-stage HLV full walk + drop load-payload PTEs + cut-at-payload (2026-08-07)
- [x] **Gap Highlighted SvH staff audit (2026-08-11):** SoT = `Hypervisor (10x) - Gap Highlighted.xlsx` sheet `SvH` — **54** Col-A coverpoints (+ 7 `***` note rows 46/59–64). Arch breakdown: **RV64=7** / **BOTH=47**. Suite **76** active `.S`. Waived: `cp_vsatp_speculative_a_bit`. Intentional: VSBE Sail; R48 HS/U/VU matrix VS-directed only.
- [x] **VM_permission write_acc close (2026-08-11):** `svh_twostage_invalid_{,sv39_}VSmode.S` cases 3–4 VS `sw` (separate hops + `hfence` + SPA check). Licensed Questa: **99.29%** RV32 (552/555) / **99.39%** RV64 (817/820). Missing bins = VSBE only.
- [x] Covergroup M1/M2: vsatp invalid/svpbmt/reserved use `priv_mode_vs`
- [x] `pbmte_menvcfg` only bins `{0}=no_support` — PBMT crosses score with PBMTE cleared (2026-08-07)
- [x] **SvHCommon.py `build_twins` string list fix (2026-08-30):** `build_twins` combines both `sv32_strs` and `sv39_strs` under their respective `#ifdef` guards using `combine_twins(sv32_strs, sv39_strs)` so symbol references (e.g. `SvH_cg_cp_hgatp_mode_field_mode_bare_sv32_str`) are preserved in `.data` for both RV32 and RV64 targets.
- [x] **SvH Test Generator Review audit (2026-08-30):** All 12 PDF items done — register allocator, `GOTO_*` / `PTE_*` constants, native SIGUPD, explicit `_emit(...)` × 39; line comments on all 7 generator files.
- [x] **doc_skills plug-in (2026-08-31):** `.cursor/doc_skills/` — portable pack for any agent/IDE. `./install.sh link /path/to/ACT4` or read pack `AGENTS.md`. See `PLUGIN.md`, `prompts/bootstrap.md`, `sync-from-repo.sh`.

## Wrong directions (refuse)
- Installing cracked Questa / license hacks
- Claiming coverage “passed” without UCDB/report from licensed sim
- Editing generated `tests/rv32*`, `coverpoints/unpriv`, `coverpoints/coverage` aggregates by hand
- Mixing hypervisor-xlsx norm pipeline commands into arch-test Make runs

## Related
- [[08-Agent-Memory-Protocol]] · [[06-Host-Env-Questa]]
